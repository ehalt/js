// -------------- null  vs undefined ---------------

// A variable without value always store 'undefined'

// A variable with unknown value can store 'null'

var abc

var xyz = null 

console.log(null)
console.log(abc)